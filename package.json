{
  "name": "plugin-release-dashboard",
  "version": "1.0.0",
  "description": "Modrinth-style plugin release management dashboard using Netlify + GitHub",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
